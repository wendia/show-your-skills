# 技能五子棋 (Show Your Skills) 设计文档

**版本**: 2.0  
**创建日期**: 2026-02-22  
**更新日期**: 2026-02-24  
**项目路径**: `~/game/show-your-skills`

---

## 目录

1. [项目概述](#1-项目概述)
2. [系统架构](#2-系统架构)
3. [前后端交互设计](#3-前后端交互设计)
4. [配置系统设计](#4-配置系统设计)
5. [主题系统设计](#5-主题系统设计)
6. [技能系统设计](#6-技能系统设计)
7. [数据流设计](#7-数据流设计)
9. [安全设计](#9-安全设计)
10. [性能优化](#10-性能优化)
11. [迭代优化建议](#11-迭代优化建议)
12. [技术债务](#12-技术债务)

---

## 1. 项目概述

### 1.1 游戏简介

**技能五子棋**是一款带技能卡的五子棋对战游戏：

- 每局开始时，双方各随机获得 **3 张技能卡**（可配置）
- 玩家可以在对局中使用技能卡改变局势
- 每张技能卡只能使用一次
- 最终胜负标准与传统五子棋相同（五子连珠获胜）

### 1.2 核心特性

| 特性 | 描述 | 状态 |
|------|------|------|
| 技能卡系统 | 5种独特技能卡 | ✅ |
| 主题配置 | 多主题切换 | ✅ v2.0 |
| 技能池配置 | 自定义技能池 | ✅ v2.0 |
| 在线对战 | 实时多人 | 🔄 |
| AI对手 | 单人模式 | 🔄 |
| 用户系统 | 账号/战绩 | 🔄 |

### 1.3 技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| **前端框架** | React | 18.2 |
| **构建工具** | Vite | 5.0 |
| **状态管理** | Zustand | 4.5 |
| **UI组件** | shadcn/ui | latest |
| **样式** | Tailwind CSS | 3.4 |
| **动画** | Framer Motion | 11.0 |
| **后端运行时** | Node.js | 22.x |
| **服务器框架** | Fastify | 4.26 |
| **数据库** | SQLite | 3.x |
| **WebSocket** | ws | 8.16 |

---

## 2. 系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户界面层                                │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  React App (Vite)                                       │    │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │    │
│  │  │   Pages      │  │  Components  │  │   Hooks      │ │    │
│  │  │  - Home      │  │  - Board     │  │  - useTheme  │ │    │
│  │  │  - Game      │  │  - SkillCard │  │  - useGame   │ │    │
│  │  │  - Lobby     │  │  - PlayerInfo│  │  - useConfig │ │    │
│  │  └──────────────┘  └──────────────┘  └──────────────┘ │    │
│  └────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────────┐
│                     配置与主题层 (v2.0)                          │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  ConfigManager                                          │    │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │    │
│  │  │ThemeManager  │  │SkillPool     │  │SkillRegistry │ │    │
│  │  │              │  │Manager       │  │              │ │    │
│  │  └──────────────┘  └──────────────┘  └──────────────┘ │    │
│  └────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────────┐
│                        状态管理层                                │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  Zustand Stores                                         │    │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │    │
│  │  │  gameStore   │  │ themeStore   │  │ configStore  │ │    │
│  │  └──────────────┘  └──────────────┘  └──────────────┘ │    │
│  └────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────────┐
│                        数据访问层                                │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  API Client                                             │    │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │    │
│  │  │  REST API    │  │  WebSocket   │  │  Config API  │ │    │
│  │  └──────────────┘  └──────────────┘  └──────────────┘ │    │
│  └────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────────┐
│                        服务器层                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  Node.js + Fastify                                      │    │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │    │
│  │  │  REST API    │  │  WebSocket   │  │  Config Svc  │ │    │
│  │  └──────────────┘  └──────────────┘  └──────────────┘ │    │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │    │
│  │  │  Game Engine │  │  Skill System│  │  Auth Svc    │ │    │
│  │  └──────────────┘  └──────────────┘  └──────────────┘ │    │
│  └────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────────┐
│                        数据层                                    │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  SQLite Database                                        │    │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │    │
│  │  │   Users      │  │   Games      │  │  Configs     │ │    │
│  │  └──────────────┘  └──────────────┘  └──────────────┘ │    │
│  └────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 技术选型理由

| 技术 | 选型理由 |
|------|---------|
| **React 18** | 生态成熟，并发渲染，适合复杂交互 |
| **TypeScript** | 类型安全，开发体验好，减少Bug |
| **Vite** | 极速HMR，开发体验优秀 |
| **Zustand** | 轻量级，API简洁，TypeScript友好 |
| **shadcn/ui** | 现代设计，完全可定制，代码在项目中 |
| **Tailwind CSS** | 原子化CSS，快速开发，易于主题化 |
| **Fastify** | 性能优秀，TypeScript支持好 |
| **SQLite** | 无需服务器，易备份，适合中小规模 |

---

## 3. 前后端交互设计

### 3.1 API架构

```
┌──────────────────────────────────────────────────────┐
│                    前端 (React)                       │
│  ┌────────────────────────────────────────────┐     │
│  │  Components                                 │     │
│  │  └─→ API Client ─→ REST / WebSocket        │     │
│  └────────────────────────────────────────────┘     │
└──────────────────────────────────────────────────────┘
                        ↓ HTTPS / WSS
┌──────────────────────────────────────────────────────┐
│                    后端 (Fastify)                     │
│  ┌────────────────────────────────────────────┐     │
│  │  REST API (HTTP)                            │     │
│  │  - 认证 /api/auth/*                         │     │
│  │  - 配置 /api/config/*                       │     │
│  │  - 游戏 /api/games/*                        │     │
│  │  - 用户 /api/users/*                        │     │
│  └────────────────────────────────────────────┘     │
│  ┌────────────────────────────────────────────┐     │
│  │  WebSocket (WSS)                            │     │
│  │  - 游戏同步 /ws                             │     │
│  │  - 实时通信                                 │     │
│  └────────────────────────────────────────────┘     │
└──────────────────────────────────────────────────────┘
```

### 3.2 REST API 设计

#### 认证接口

| 端点 | 方法 | 描述 |
|------|------|------|
| `/api/auth/register` | POST | 用户注册 |
| `/api/auth/login` | POST | 用户登录 |
| `/api/auth/logout` | POST | 用户登出 |
| `/api/auth/me` | GET | 获取当前用户 |

#### 配置接口 (v2.0)

| 端点 | 方法 | 描述 |
|------|------|------|
| `/api/config/themes` | GET | 获取所有主题 |
| `/api/config/themes/:id` | GET | 获取主题详情 |
| `/api/config/skillPools` | GET | 获取所有技能池 |
| `/api/config/skillPools/:id` | GET | 获取技能池详情 |

#### 游戏接口

| 端点 | 方法 | 描述 |
|------|------|------|
| `/api/games` | GET | 获取游戏历史 |
| `/api/games/:id` | GET | 获取游戏详情 |
| `/api/leaderboard` | GET | 获取排行榜 |

### 3.3 WebSocket 消息协议

#### 客户端 → 服务器

| 类型 | 描述 | 数据 |
|------|------|------|
| `ping` | 心跳 | `{ timestamp }` |
| `create_room` | 创建房间 | `{ config: { skillPoolId, themeId } }` |
| `join_room` | 加入房间 | `{ roomId }` |
| `find_game` | 匹配游戏 | `{ preferences }` |
| `move` | 落子 | `{ position }` |
| `skill` | 使用技能 | `{ skillCardId, targetPosition }` |
| `chat` | 聊天 | `{ message }` |
| `undo_request` | 悔棋请求 | - |
| `resign` | 投降 | - |

#### 服务器 → 客户端

| 类型 | 描述 | 数据 |
|------|------|------|
| `pong` | 心跳响应 | `{ timestamp }` |
| `room_created` | 房间创建成功 | `{ roomId, config }` |
| `room_joined` | 加入房间成功 | `{ roomId, color, opponent }` |
| `game_start` | 游戏开始 | `{ roomId, gameState }` |
| `state_update` | 状态更新 | `{ gameState }` |
| `game_over` | 游戏结束 | `{ winner, reason }` |
| `error` | 错误 | `{ code, message }` |

### 3.4 状态同步机制

```
┌──────────┐                    ┌──────────┐
│  Client  │                    │  Server  │
│  Player  │                    │ Authority│
└────┬─────┘                    └────┬─────┘
     │                               │
     │  1. 用户操作 (点击棋盘)        │
     │─────────────────────────────>│
     │                               │
     │  2. 乐观更新 UI               │
     │  (立即显示棋子)               │
     │                               │
     │  3. 服务器验证                │
     │                               │
     │  4a. 验证通过                 │
     │  state_update                │
     │<─────────────────────────────│
     │                               │
     │  4b. 验证失败                 │
     │  state_rollback              │
     │<─────────────────────────────│
     │                               │
     │  5. 更新UI到权威状态          │
     │                               │
```

---

## 4. 配置系统设计

### 4.1 配置架构

```
┌──────────────────────────────────────────────────────┐
│                    配置系统                           │
│  ┌────────────────────────────────────────────┐     │
│  │  Config Files (JSON)                        │     │
│  │  ┌──────────────┐  ┌──────────────┐        │     │
│  │  │  themes/     │  │ skillPools/  │        │     │
│  │  │  - default   │  │ - standard   │        │     │
│  │  │  - magic     │  │ - chaos      │        │     │
│  │  │  - tech      │  │ - tactical   │        │     │
│  │  └──────────────┘  └──────────────┘        │     │
│  └────────────────────────────────────────────┘     │
│                       ↓                             │
│  ┌────────────────────────────────────────────┐     │
│  │  Config Manager                             │     │
│  │  - loadConfig()                             │     │
│  │  - getTheme(id)                             │     │
│  │  - getSkillPool(id)                         │     │
│  └────────────────────────────────────────────┘     │
│                       ↓                             │
│  ┌────────────────────────────────────────────┐     │
│  │  API Layer                                  │     │
│  │  - GET /api/config/themes                   │     │
│  │  - GET /api/config/skillPools               │     │
│  └────────────────────────────────────────────┘     │
└──────────────────────────────────────────────────────┘
```

### 4.2 主题配置

```json
{
  "id": "magic",
  "name": "魔法世界",
  "version": "1.0.0",
  
  "rarity": {
    "common": {
      "gradient": "from-slate-600 to-slate-700",
      "border": "border-slate-500/30",
      "glow": "rgba(100,116,139,0.3)"
    },
    "legendary": {
      "gradient": "from-amber-500 to-orange-600",
      "border": "border-amber-400/50",
      "glow": "rgba(245,158,11,0.6)"
    }
  },
  
  "card": {
    "borderRadius": 16,
    "shadowGlow": true
  },
  
  "board": {
    "style": "wood",
    "gridColor": "#8B4513"
  }
}
```

### 4.3 技能池配置

```json
{
  "id": "standard",
  "name": "标准技能池",
  
  "skills": [
    {
      "id": "reverse_chaos",
      "name": "倒转乾坤",
      "rarity": "legendary",
      "effectId": "flipStones",
      "params": { "flipPercent": 30 }
    }
  ],
  
  "distribution": {
    "method": "balanced",
    "countPerPlayer": 3
  }
}
```

---

## 5. 主题系统设计

### 5.1 主题切换流程

```
┌──────────┐                    ┌──────────┐
│  Client  │                    │  Server  │
└────┬─────┘                    └────┬─────┘
     │                               │
     │  GET /api/config/themes       │
     │─────────────────────────────>│
     │                               │
     │  [themes list]                │
     │<─────────────────────────────│
     │                               │
     │  用户选择主题                 │
     │                               │
     │  setTheme('magic')            │
     │  - 加载主题配置               │
     │  - 应用CSS变量               │
     │  - 更新组件样式               │
     │                               │
```

### 5.2 主题化组件

```tsx
// 主题化技能卡
const SkillCard = ({ skill }) => {
  const rarityStyle = useRarityStyle(skill.rarity);
  
  return (
    <Card className={cn(
      `bg-gradient-to-br ${rarityStyle.gradient}`,
      `border-2 ${rarityStyle.border}`
    )}>
      {/* 卡片内容 */}
    </Card>
  );
};
```

---

## 6. 技能系统设计

### 6.1 技能执行流程

```
┌──────────┐                    ┌──────────┐
│  Client  │                    │  Server  │
└────┬─────┘                    └────┬─────┘
     │                               │
     │  使用技能卡                   │
     │  { type: 'skill',             │
     │    skillCardId: 'xxx' }       │
     │─────────────────────────────>│
     │                               │
     │  1. 验证技能卡所有权          │
     │  2. 检查使用条件              │
     │  3. 获取技能定义              │
     │  4. 执行技能效果              │
     │  5. 更新游戏状态              │
     │                               │
     │  state_update                 │
     │  { gameState: {...} }         │
     │<─────────────────────────────│
     │                               │
     │  更新UI                       │
     │  播放动画                     │
     │                               │
```

### 6.2 技能效果解耦

```typescript
// 效果定义（复用）
const flipStonesEffect = {
  id: 'flipStones',
  execute(context, params) {
    const { flipPercent = 30 } = params;
    // 执行反转逻辑
  }
};

// 配置驱动
{
  "effectId": "flipStones",
  "params": { "flipPercent": 50 }  // 可配置参数
}
```

---

## 7. 数据流设计

### 7.1 游戏数据流

```
用户操作 → 组件 → Store → API Client → Server
                                        ↓
                                    验证/处理
                                        ↓
UI更新 ← 组件 ← Store ← API Client ← Server响应
```

### 7.2 配置数据流

```
应用启动 → 加载配置 → 缓存配置 → 应用主题
              ↓
         监听配置变化
              ↓
         热更新UI
```

---

## 9. 安全设计

### 9.1 前端安全

| 措施 | 描述 |
|------|------|
| 输入验证 | 所有用户输入验证 |
| XSS防护 | 避免dangerouslySetInnerHTML |
| 敏感数据 | 不在前端存储敏感数据 |
| Token管理 | 安全存储，自动刷新 |

### 9.2 后端安全

| 措施 | 描述 |
|------|------|
| 认证中间件 | 所有API验证Token |
| 速率限制 | 防止DDoS攻击 |
| 输入验证 | 所有输入验证 |
| SQL注入防护 | 参数化查询 |
| 服务器权威 | 游戏逻辑在服务器执行 |

---

## 10. 性能优化

### 10.1 前端优化

| 优化项 | 方案 |
|--------|------|
| 代码分割 | React.lazy + Suspense |
| 图片优化 | 懒加载 + WebP格式 |
| 动画优化 | Framer Motion + GPU加速 |
| 状态优化 | Zustand + 选择性订阅 |
| 缓存策略 | 配置缓存 + 本地存储 |

### 10.2 后端优化

| 优化项 | 方案 |
|--------|------|
| 数据库 | WAL模式 + 索引优化 |
| 配置缓存 | 内存缓存 + 定时刷新 |
| WebSocket | 心跳机制 + 连接池 |
| 响应压缩 | Gzip压缩 |

---

## 11. 迭代优化建议

### 11.1 短期优化 (1-2周)

#### 性能优化
- [ ] 实现前端代码分割
- [ ] 添加图片懒加载
- [ ] 优化动画性能
- [ ] 添加配置缓存

#### 用户体验
- [ ] 添加加载状态
- [ ] 优化错误提示
- [ ] 添加音效反馈
- [ ] 优化移动端适配

### 11.2 中期优化 (1-2月)

#### 功能增强
- [ ] 添加更多主题
- [ ] 添加更多技能卡
- [ ] 实现AI对手
- [ ] 添加观战模式

#### 技术改进
- [ ] 添加单元测试
- [ ] 添加E2E测试
- [ ] 实现CI/CD
- [ ] 添加监控告警

### 11.3 长期优化 (3-6月)

#### 架构升级
- [ ] 考虑迁移到PostgreSQL（如果需要）
- [ ] 实现微服务架构（如果需要）
- [ ] 添加Redis缓存（如果需要）
- [ ] 实现容器化部署

#### 功能扩展
- [ ] 添加排位赛系统
- [ ] 添加好友系统
- [ ] 添加锦标赛系统
- [ ] 添加成就系统

### 11.4 技术迭代建议

#### 前端技术迭代

| 阶段 | 技术 | 原因 |
|------|------|------|
| 短期 | 添加测试 | 保证代码质量 |
| 中期 | 升级React 19 | 新特性支持 |
| 长期 | 考虑SSR | SEO优化 |

#### 后端技术迭代

| 阶段 | 技术 | 原因 |
|------|------|------|
| 短期 | 添加日志系统 | 问题排查 |
| 中期 | 添加监控 | 性能优化 |
| 长期 | 考虑微服务 | 可扩展性 |

---

## 12. 技术债务

### 12.1 当前技术债务

| 债务 | 影响 | 优先级 |
|------|------|--------|
| 缺少测试 | 代码质量风险 | 高 |
| 缺少监控 | 问题排查困难 | 中 |
| 缺少文档 | 维护成本高 | 中 |

### 12.2 偿还计划

| 阶段 | 任务 | 时间 |
|------|------|------|
| Week 1 | 添加核心功能测试 | 3天 |
| Week 2 | 添加监控和日志 | 2天 |
| Week 3 | 完善文档 | 2天 |

---

## 13. 更新日志

### v2.0 (2026-02-24)

- ✅ 添加配置系统设计
- ✅ 添加主题系统设计
- ✅ 添加技能池系统设计
- ✅ 重构前后端交互设计
- ✅ 添加迭代优化建议

### v1.0 (2026-02-22)

- ✅ 初始设计文档
- ✅ 基础游戏逻辑
- ✅ 5种技能卡实现

---

## 14. 参考文档

- [前端设计文档](design/FRONTEND_DESIGN.md)
- [服务器设计文档](design/SERVER_DESIGN.md)
- [API接口文档](design/API.md)
- [部署文档](design/DEPLOYMENT.md)
- [工作计划](WORKPLAN.md)

---

**文档维护者**: Elwen
**最后更新**: 2026-02-24
